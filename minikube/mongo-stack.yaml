apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-mongodb-deployment
  labels:
    app: order-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-mongodb
  template:
    metadata:
      labels:
        app: order-mongodb
    spec:
      containers:
      - name: rvstore-orders-mongodb
        image: mongo
        envFrom:
        - configMapRef:
            name: mongodb-config
# Commented until you update the colume path at the bottom to be a valid file path. Then uncomment all these lines.
#        volumeMounts:
#        - mountPath: /data/db
#          name: mongo-volume
        ports:
        - containerPort: 27017
#      volumes:
#      - name: mongo-volume
#        hostPath:
#          path: /Users/tsolley/Desktop/Temp/rvstore-mongodb
